function AboutPage() {
  return (
    <section className="card">
      <h1>About Page</h1>
      <p>
        This starter project uses React (frontend) and Django (backend) with a
        clean folder structure for quick development.
      </p>
    </section>
  )
}

export default AboutPage
